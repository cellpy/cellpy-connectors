# Issue #3 status

- [x] Done

## What's done

- Captured the GitHub issue and confirmed the plan (Typer `configure` on the
  existing app; no console script; no `metadata_sources` entry point).
- Credential resolver, `ConnectorError` hierarchy, `ApiClientBase`, empty
  connector registry, and `cellpy connectors configure <name>`.
- Tests: `uv run pytest` — 22 passed (including live cellpy mount tests).
- README, `this-project.md`, test registry, and `connector-base.md`.
- Close: no `HISTORY.md` at the project root, so the changelog step was skipped.

## Remaining work

None.
